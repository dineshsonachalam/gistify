apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.anyJsonBackend.appName }}
  namespace: {{ .Values.namespace }}
spec:
  entryPoints:
    - web
  routes:
  - match: {{ .Values.anyJsonBackend.ingressRoute }}
    kind: Rule
    services:
    - name: {{ .Values.anyJsonBackend.appName }}
      port: {{ .Values.anyJsonBackend.containerPort }}   