# 1. Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Values.anyJsonFrontend.appName }}
  name: {{ .Values.anyJsonFrontend.appName }}
  namespace: {{ .Values.namespace }}
spec:  
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.anyJsonFrontend.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.anyJsonFrontend.appName }}
    spec:
      containers:
      - name: {{ .Values.anyJsonFrontend.appName }}
        image: {{ .Values.anyJsonFrontend.image }}
        imagePullPolicy: Always          
        ports:
        - containerPort: {{ .Values.anyJsonFrontend.containerPort }}   
          name: {{ .Values.anyJsonFrontend.appName }}   
